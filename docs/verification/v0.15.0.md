# v0.15.0 verification

- `task_id`: jev-first-20260920
- `artifact_paths`: `skills/agentic-vault/scripts/jev_ask.py`, `commands/jev-ask.md`, shared skill and Codex routing, `docs/jev-judgments.md`, direct adapter tests, v0.15.0 metadata.
- `verification_commands_and_results`: `python -X utf8 -m unittest discover -s tests -v`: 495 tests, 480 passed, 15 platform skips, 0 failures (166.661 seconds). Independent focused direct/client/file/metadata tests: 78 passed. Recall fixtures 16/16, recall@3 and MRR 1.0; compileall, skill validation and whitespace checks passed. The documented examples prepared 1 direct question, all 3 native types, and all 7 legacy file tasks offline. All 78 relative documentation links resolved. Both Python and Node direct CLIs passed the same offline prepare/missing-key fixtures. One public synthetic live batch through this CLI returned Noul 0.97 for ordinary 1+1=2, Choice two, and a valid greeting Score; no private material was sent. These examples do not establish task accuracy.
- `verified_by`: Codex `/root/jev_first_harness`, independent read-only review on 2026-09-20: 78 focused tests passed; native types, input/response guards, routing and unchanged legacy source policies checked. Full-suite results were separately collected by `/root/jev_first_vault/vault_docs`; root collected public CLI smoke results.
- `assumptions`: the current request falls within explicit Jev-first and selected-input transmission authorization; inline context is not file provenance. Existing file-bound contracts and permission/state writers stay compatible.
- `unresolved`: exact-commit remote CI and installation are verified separately in release assets; representative task accuracy and automatic routing in a fresh host session are not established by these tests. Python transport bounds socket operations/body size but does not promise a hard OS-level DNS/header deadline.
- `next_safe_action`: verify the exact commit in the remote CI matrix, publish verified assets, and update host installations while preserving settings. Start a new host session to load the updated skill.
