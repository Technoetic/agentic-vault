# v0.15.1 verification

- `task_id`: agentic-vault-eval-fix-20260923
- `artifact_paths`: `skills/agentic-vault/scripts/jarvis_bridge.py` (stdin prompt, Windows batch-launcher refusal, `--tools`/`--strict-mcp-config`, update isolation, retry backoff, hidden console), `jev_client.py`/`jev_ask.py`/`vault_judge.py` (one secret filter), `vault_paths.py`/`vault_healthcheck.py`/`backup_vault.py`/`vault_recall.py` (one path rule, §14b, utf-8-sig), `tests/test_jarvis_bridge.py`, `tests/test_rule_sources.py`, `tests/test_readme_truth.py`, README badges and English overview, `SECURITY.md`, `docs/README.md`, `.github/workflows/tests.yml`, v0.15.1 metadata.
- `verification_commands_and_results` (Windows 11, Python 3.12, local worktree):
  - `python -m unittest discover -s tests` with `PYTHONUTF8=1`: rc 0, 564 tests, OK, 17 platform skips (public v0.15.0 had 495 tests).
  - `python scripts/evaluate_recall.py`: rc 0, 16 queries, recall@3 1.0, MRR 1.0, no failed queries.
  - `python -m compileall -q hooks skills/agentic-vault/scripts scripts`: rc 0.
  - `git diff --check 4b825dc642cb6eb9a060e54bf8d69288fbee4904 HEAD` (the new CI step, whole committed tree): rc 0 on the release commit. In a scratch repository with a committed trailing space the same command returned rc 2 while the old bare `git diff --check` returned rc 0.
- Security regression coverage (all offline, fake executables and mocks): the prompt reaches the child only on stdin; argv stays fixed for messages with quotes, `&`, `|`, `>`, line feeds and a leading `-`; a real fake `claude.cmd` placed first on `PATH` is never executed on Windows; a POSIX child reads the prompt from stdin; `--tools Read,Grep,Glob`, `--allowedTools` and `--strict-mcp-config` are present; control characters in argv settings are refused.
- Review follow-up coverage: the full argv is pinned (no `--setting-sources` or `--restricted`, see SECURITY.md); a 300 KB prompt to a real child that never reads stdin returns the timeout message after `qa_timeout_sec` (a scratch run of the earlier `subprocess.run(input=...)` path took 30.2 s against 1.0 s now); stdin carries exact UTF-8 bytes; an update that raises is retried and skipped only after three consecutive exceptions and a delivered notice; a transient capture error is retried and the capture saved; the same secret and benign fixtures give the same verdict on the `jev_client`, `jev_ask` and `vault_judge` paths, including Telegram, Slack, Google, URL-credential, Korean-label, full-width and zero-width cases. The Jarvis test module points `STATE_ROOT` at a temporary directory; the size and mtime of the real `~/.vault-jarvis/jarvis.log` were unchanged across a full run (earlier runs appended 12 fixture lines per run).
- `verified_by`: not yet. An independent read-only session should re-run the commands above and append its result here.
- `assumptions`: the flags `--tools` and `--strict-mcp-config` exist in the installed Claude CLI. This was checked with `claude --help` of Claude Code 2.1.252 during the evaluation (no model call). No test launched the real Claude CLI, Telegram or the Jev API.
- `unresolved`: remote CI on the exact release commit has not run. Codex and Claude Code install and discovery were not re-measured for this patch; the last such record is [v0.15.0](v0.15.0.md). The `v0.15.1` tag and GitHub release do not exist yet, so README links to them resolve only after the owner publishes them. Private vulnerability reporting must be enabled in the repository settings for the SECURITY.md link to work.
- `next_safe_action`: independent re-run of the commands, owner review, then (owner decision) push, tag `v0.15.1`, run CI on the tag and restart any running Jarvis bridge.
