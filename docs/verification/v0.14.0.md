# v0.14.0 verification

- `task_id`: agentic-vault-jev-judge-20260920
- `artifact_paths`: `skills/agentic-vault/scripts/{vault_judge,jev_client}.py`, `commands/vault-judge.md`, `docs/jev-judgments.md`, shared skill and Codex routing, associated tests.
- `verification_commands_and_results`: final `python -X utf8 -m unittest discover -s tests -q` ran **473 tests: 458 passed, 15 platform exclusions, zero failures** (146.628 seconds, Windows/Python 3.12). Baseline was 426 total/411 passed/15 excluded. Focused client and binding tests: **47 passed**, independently rerun. Seven documented synthetic offline examples: **7/7 prepared**. Two live API calls using only public synthetic English/Korean evidence produced **16/16 expected choices**, including two missing-evidence abstentions (`needs_review`); this does not establish business-task accuracy. Lexical recall benchmark: 16/16, recall@3 and MRR 1.0. Skill/plugin validation, compileall and diff check passed. Commit-specific CI and installation evidence are recorded with the release.
- `assumptions`: external transmission is authorized for selected source excerpts; API fixture checks test protocol/connectivity, not general model accuracy. Host routing follows the installed skill rather than a universal hook.
- `unresolved`: representative business-task accuracy, cost benefit and hard wall-clock DNS/header deadlines are not established. Existing Windows link/symlink platform exclusions remain explicit.
- `next_safe_action`: publish the verified commit after CI checks and update installed plugins, preserving existing vault settings and activation choices.
- `verified_by`: `/root/vault_jev_fit`, **2026-09-20 17:16 KST**, independent focused tests **47/47**, routing scenarios and adversarial filesystem/input/response probes passed. Found a Windows 8.3 alias that read an excluded source before returning an error; regression test reproduced the issue and the canonical-path exclusion fix was reverified at `os.open` level: **source opens 0, network calls 0**. Real NTFS junction rejection passed; file/directory symlink creation was unavailable in this environment. Final runtime SHA256: `vault_judge.py` **929ad0e6afc0ed2a66720f8968b62985b51296a9e1aa349fcf8b8322c65e9e0b**; `jev_client.py` **9cf6ce1d45b07aa180546f9463410b21dfe2b062f9a9e9052123718ee3d9ad9d**.
