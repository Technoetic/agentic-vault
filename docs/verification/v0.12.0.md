# v0.12.0 verification

Date: 2026-09-16. Previous public source: `1fbff5c` (v0.11.0).

task_id: `agentic-vault-v0.12.0`

artifact_paths:

- `assets/templates/rules/vault-browser.md` (new, `engine=0.12.0`)
- `commands/vault-init.md`, `commands/vault-upgrade.md`, `assets/templates/CLAUDE-vault-stub.md`, `assets/templates/AGENTS-vault-stub.md`
- `README.md`, `docs/codex.md`, `docs/releases/v0.12.0.md`
- Claude/Codex plugin manifests and `tests/test_release_metadata.py`

verification_commands_and_results:

| Check | Observed result |
|---|---|
| `python -X utf8 -m unittest discover -s tests -v` | Ran 410 tests in 86.876s: OK (skipped=15) |
| `scan_rules_integrity` — six templates + CLAUDE stub + a vault-specific browser clause titled `브라우저 실행 기본값` | oversized [], duplicate headings [] |
| `scan_rules_integrity` — six templates + a legacy monolithic CLAUDE.md (12 sections, rules never split) | oversized []; 5 duplicate headings, all from the five pre-existing templates (expected for an unmigrated monolith); `vault-browser.md` duplicates: none |
| Unchanged rule templates | five existing templates keep their prior `engine=` stamps (asserted by `test_v0120_browser_rule_is_wired`) |

assumptions:

- The rule states boundaries only; tool choice, executable path and account stay in each vault's CLAUDE.md.
- Where page data goes in each tool mode is not verified by the engine; rule 3 requires confirming before opening sensitive pages.

unresolved:

- No automated `/vault-upgrade` run adds the file to an existing vault; the command document was updated and the next real upgrade is the check.
- Vaults still on the monolithic CLAUDE.md contract receive the rule only through the 1-1 migration.

next_safe_action: tag `v0.12.0` from this commit after review, publish the release, then run `/vault-upgrade` on one operating vault and record the added file in that vault's log.
