# v0.13.0 verification

Date: 2026-09-20. Previous public source: `82b0d27` (v0.12.0).

task_id: `agentic-vault-v0.13.0`

artifact_paths:

- `skills/agentic-vault/scripts/vault_healthcheck.py`, `assets/templates/vault-config.json`
- `tests/test_action_gates.py`, `tests/test_release_metadata.py`
- Claude/Codex plugin manifests, `README.md`, `docs/codex.md`, `docs/releases/v0.13.0.md`

verification_commands_and_results:

| Check | Observed result |
|---|---|
| `python -B -X utf8 -m unittest discover -s tests -v` | 426 tests in 114.465s: 411 passed, 15 environment skips, 0 failures |
| `python -B -X utf8 scripts/evaluate_recall.py` | 16 queries (8 Korean, 8 English), recall@3 = 1.0, MRR = 1.0, no failures or evaluation errors |
| `python -X utf8 -m compileall -q hooks skills/agentic-vault/scripts scripts` | exit 0 |
| Codex `plugin-creator/scripts/validate_plugin.py` against this repository | Plugin validation passed |
| `git diff --check` | exit 0 |
| Independent `python -B -X utf8 -m unittest tests.test_release_metadata tests.test_action_gates -v` | 25 tests passed, exit 0 |

verified_by: Codex read-only reviewer `/root/gates_independent_review`, 2026-09-20 12:19 KST. Reviewed the feature diff and final release metadata, reran focused tests, and confirmed the upgrade version-stamp correction. Reviewed healthcheck SHA-256: `4bfe74fac1c38db26bf1753c4861146d7319a46e32b5141c296a928d5badc140`.

assumptions:

- Action gates validate policy declarations only; no runtime authorization or enforcement is included.
- Existing network pre-push restrictions remain independent of this configuration.

unresolved:

- Runtime consumers for action-policy declarations are outside this release.
- Existing vaults with locally modified standalone healthcheck scripts require review during upgrade.

next_safe_action: publish the verified release candidate, then verify its public tag and installed plugin versions.
