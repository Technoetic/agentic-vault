# v0.11.0 verification

Date: 2026-09-10. Previous public source: `351cef7` (v0.10.0).
The evidence implementation is `cd1a9da`, followed by the historical-hash
documentation clarification in `3b9eace`.

task_id: `agentic-vault-v0.11.0`

artifact_paths:

- `skills/agentic-vault/scripts/vault_evidence.py`, `tests/test_vault_evidence.py`
- `scripts/verify_codex_plugin.py`, `tests/test_codex_verification.py`
- Claude/Codex plugin manifests and `tests/test_release_metadata.py`
- `README.md`, `docs/codex.md`, `docs/evidence.md`, `docs/releases/v0.11.0.md`
- Shared skill, Codex routing, session commands and handoff template
- [Implementation verification](evidence-handoff.md), preserved as a historical checkpoint

verification_commands_and_results:

| Check | Observed result |
|---|---|
| `python -X utf8 -m unittest discover -s tests -v` | 408 tests in 95.902 seconds: 393 passed, 15 environment skips, no failures/errors |
| Independent evidence tests | 55 tests in 36.035 seconds: 53 passed, 2 Windows environment skips, no failures |
| New installer regressions before correction | Nested worktree copied; four changed/missing evidence-resource subcases accepted; five failing assertions |
| Independent installer tests after correction | 9 tests: 7 passed, 2 POSIX-only skips, no failures |
| Independent release metadata tests | 7 passed |
| `python scripts/evaluate_recall.py` | 16 bilingual lexical queries; recall@3 and MRR both 1.0; no failed queries or evaluation errors |
| `python -m compileall -q hooks skills/agentic-vault/scripts scripts` | Exit 0 |
| Plugin manifest and shared-skill validators | Passed |
| `claude plugin validate --strict` on manifest, commands and skills | All three passed |
| `python -X utf8 scripts/verify_codex_plugin.py` | v0.11.0 installed in a disposable Codex home; shared skill and two SessionStart hooks discovered; evidence engine and documentation byte comparison passed; zero model turns; hooks untrusted |
| `git diff --check` | Exit 0 |

The release adds two installer regressions to the 406-test implementation checkpoint.
The installer now excludes `.worktrees` and `worktrees` at every copied depth and
checks both new evidence resources in the installed cache. The evidence engine
and its 55 behavioral tests are unchanged by release packaging.

verified_by:

- Codex `/root/release_audit`, 2026-09-10 20:10 KST: read-only public-content and
  evidence-code review; independent 55-test run, no release-blocking findings.
- Codex `/root/release_audit`, 2026-09-10 20:12 KST: final metadata/documentation
  review and independent installer/metadata test runs, no blocking findings.

assumptions:

- Local execution uses Windows Python 3.12.10 and Codex CLI 0.150.1. Hosted CI
  results are separate evidence and must identify the exact published commit.
- Publication is gated on the existing Windows/macOS/Linux by Python
  3.10/3.12/3.14 matrix. The completed run is linked from the
  [GitHub release](https://github.com/Technoetic/agentic-vault/releases/tag/v0.11.0).
- Plugin installation does not authorize hook trust, a vault migration, report
  instructions or changes to user-owned notes. Enabled states must be checked
  after switching marketplace sources; old caches may be removed by the installer.

unresolved:

- The two new-feature Windows skips are symlink privileges and a case-sensitive
  filesystem. The distinct-case missing-binding regression also has a
  platform-independent PurePosix receipt check.
- File bindings are point-in-time observations. They do not authenticate the
  verifier, prove command execution or claim truth, freeze a filesystem, or
  capture undeclared dependencies.
- Model selection of the skill in a fresh conversation and trusted hook execution
  are not exercised by the zero-model-turn installation verifier.
- No multi-session quality, elapsed-time or token-saving improvement is claimed.

next_safe_action: Publish the tested commit after its hosted matrix passes, then
install from the v0.11.0 tag and verify the actual cache while preserving existing
enabled states and hook trust. Use snapshot before verification, then record and
read the current handoff alongside the user's current requirements.
