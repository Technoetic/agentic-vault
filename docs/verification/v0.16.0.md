# v0.16.0 verification

- `task_id`: run-discipline-20260925
- `artifact_paths`: `assets/templates/rules/vault-workflow.md`, `assets/templates/rules/vault-collab.md` (both `engine=0.16.0`), `commands/vault-session-end.md`, `vault-session-start.md`, `vault-trace.md`, `vault-recall.md`, `vault-lint.md`, `vault-process-inbox.md`, `vault-ingest.md`, `vault-upgrade.md`, `vault-init.md`, `vault-jarvis-setup.md`, handoff/tasks templates, `skills/agentic-vault/SKILL.md`, `references/memory-tiers.md`, `jarvis_bridge.py` (briefing prompt text only), `docs/reliability.md`, `SECURITY.md` Korean summary, README, `tests/test_release_metadata.py`, v0.16.0 metadata.
- `verification_commands_and_results` (Windows 11, Python 3.12.10, local worktree on `feat/v0.16.0-run-discipline` from `ff9ff1d`, uncommitted, 2026-09-25 03:16 KST):
  - `python -m unittest discover -s tests` with `PYTHONUTF8=1`: rc 0, 566 tests, OK, 17 platform skips (565 before this change; the new test is `test_v0160_run_discipline_is_wired`).
  - `python scripts/evaluate_recall.py`: rc 0, 16 queries, recall@3 1.0, MRR 1.0.
  - `python -m compileall -q hooks skills/agentic-vault/scripts scripts`: rc 0.
  - `git diff --check` on the working tree: rc 0. The CI whole-tree form (`git diff --check 4b825dc642cb6eb9a060e54bf8d69288fbee4904 HEAD`) needs the release commit and has not run.
- Review coverage: before editing, each playbook recommendation was mapped onto the repo and checked by two independent read-only verifiers (gap is real / fits the vault engine). After editing, two read-only review passes (fidelity to the verified drafts, contract conflicts, release completeness; then the fix and optional-item delta) raised 39 findings. Each non-nit finding was re-checked by a separate skeptic. The 20 that held, plus most nits, were fixed; `git push --mirror` was left out of the force-push list on purpose because it could be misread as covering the sanctioned push to the `mirror` remote.
- `verified_by`: Claude Code session (Claude Opus 5.5), 2026-09-25, same environment as above. The reviewers were separate subagents without shell access; they did not run commands.
- `assumptions`: the new clauses are prose rules the model follows. Tests check that the wording and version surfaces are in place, not how a model behaves. No real Claude CLI, Codex, Telegram or Jev call was made.
- `unresolved`: whether the rules hold after context compaction, whether subagent-report cross-checking and the lint cross-check are followed in real sessions, and whether a host without image reading takes the new process-inbox skip branch were not measured. The `docs/reliability.md` model-switch paragraph describes unverified host behavior. Remote CI on the release commit has not run. Codex install and discovery were not re-measured; the last record is [v0.15.0](v0.15.0.md). The `v0.16.0` tag and GitHub release do not exist yet.
- `next_safe_action`: owner review of the diff, then (owner decision) commit, push, tag `v0.16.0`, run CI on the tag, run `/vault-upgrade` in existing vaults and restart any running Jarvis bridge.
